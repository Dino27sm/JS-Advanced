function sumFirstLast(inputArray){
    let firstNum = Number(inputArray[0]);
    let lastNum = Number(inputArray[inputArray.length - 1]);
    console.log(firstNum + lastNum);
}

sumFirstLast(['5', '10']);
